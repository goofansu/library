syntax = "proto3";

package library;

service BookService {
  rpc GetBook(BookReq) returns (BookResp) {}
}

message BookReq {
  string isbn = 1;
}

message BookResp {
  string isbn = 1;
  string title = 2;
  string image = 3;
  string info = 4;
}
